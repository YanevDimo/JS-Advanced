const {lookupChar} = require('./charLookup');
const expect = require('chai').expect;

describe('lookupChar test', () => {
    it('should be first parameter valid', function () {
           expect(lookupChar('sex',1)).to.equal('valid')
    });
    it('should be second parameter valid', function () {

    });
    it('should be second parameter valid integer', function () {

    });
    it('should be value bigger than string length', function () {

    });
    it('should be value with negative index ', function () {

    });
    it('should value parameters be valid', function () {

    });

})